import 'package:flutter/cupertino.dart';

class SettingPage extends StatelessWidget{
  static const String settingHeadline = 'Settings';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('SETTING PAGE'),
    );
  }
}